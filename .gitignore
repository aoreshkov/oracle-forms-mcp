# Build
.gradle/
build/
out/

# IDE
.idea/
*.iml

# Kotlin
.kotlin/

# Local machine config — never commit
local.properties

# Private analysis/plan documents — never commit (see shared conventions)
docs/

# Claude Code — keep shareable config (skills/, agents/, rules/, settings.json) tracked;
# ignore only personal/local files
.claude/settings.local.json
.claude/plans/
CLAUDE.local.md

# Logs & OS noise
*.log
.DS_Store
Thumbs.db
captures/
